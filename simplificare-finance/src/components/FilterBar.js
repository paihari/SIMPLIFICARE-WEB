import React from 'react';
import { Box } from '@mui/material';

// FilterBar Component, now simplified with no slider
const FilterBar = () => {
  return (
    <Box mt={3}>
      {/* You can add any additional filter-related components here in the future */}
    </Box>
  );
};

export default FilterBar;
